define(function(require, exports, module) {
	var $ = require("jquery"), common = require("common"), commonDate = require("commonDate"), layer = require("layer");
	/**
	 * <pre>
	 * 获取所有智能插座@param showListDivId 克隆后加入区域的id
	 * @param options ajax 请求的参数
	 * </pre>
	 */
	exports.findAllScoket = function(cloneTempleteDivId, showListDivId,
			options, cloneCallback) {

	}
	
	
	
})